<%= form_for(@aha) do |f| %>
  <% if @aha.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aha.errors.count, "error") %> prohibited this aha from being saved:</h2>

      <ul>
      <% @aha.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_field :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
